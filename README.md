Getting Started

To set up and run this project locally, please follow the steps below:

1. Install Dependencies
   Run the following command to install all required dependencies:

   composer install

2.Environment Configuration
  Create a copy of the environment file:

  cp .env.example .env

3.Generate Application Key
  Generate the application key using:
  
  php artisan key:generate

4.Run the Application
  Start the development server:

  php artisan serve

Once the server is running, you can access the project in your browser.

Important Note

To obtain the database file or credentials, please contact:
gagandeepattri8@gmail.com
